name: Issue Triage

on:
  issues:
    types: [opened]

permissions:
  contents: read
  issues: write

jobs:
  triage:
    uses: astralform-ai/.github/.github/workflows/issue-triage.yml@main
    secrets: inherit
