name: PR Review

on:
  pull_request:
    types: [opened]

permissions:
  contents: read
  pull-requests: write
  id-token: write
  actions: read

jobs:
  review:
    uses: astralform-ai/.github/.github/workflows/claude-review.yml@main
    secrets: inherit
